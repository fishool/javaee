server:
  port: 9004

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848

# 暴露监控端点， 用于更新最新配置
management:
  endpoints:
    web:
      exposure:
        include: "*"